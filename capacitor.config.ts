import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.svgtrace.svgtrace',
  appName: 'SvgTrace',
  webDir: 'build',
  bundledWebRuntime: true
};

export default config;
